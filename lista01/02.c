#include <stdio.h>

int main(){
	int x, y, z;
	printf("Insira o valor de x:");
	scanf("%d", &x);
	printf("\nInsira o valor de y:");
	scanf("%d", &y);
	z = x + y;
	printf("O resultado da soma é: %d", z);
	return 0;
}
